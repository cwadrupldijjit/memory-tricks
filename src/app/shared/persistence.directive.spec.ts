import { PersistenceDirective } from './persistence.directive';

describe('PersistenceDirective', () => {
  it('should create an instance', () => {
    const directive = new PersistenceDirective();
    expect(directive).toBeTruthy();
  });
});
